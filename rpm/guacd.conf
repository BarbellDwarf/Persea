# persea RPM: guacd config. The unit passes all runtime flags on the
# command line; this file only needs to exist (guacd aborts when the
# default path is unreadable) and mirrors the unit's binding.
bind_host = 127.0.0.1
bind_port = 4822
