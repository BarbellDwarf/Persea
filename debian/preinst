#!/bin/sh
set -e

# Create persea system user if it doesn't exist.
# Chromium's crashpad requires a real home directory.
if ! getent passwd persea >/dev/null 2>&1; then
    adduser --system --group --home /home/persea --quiet persea
fi

#DEBHELPER#

exit 0
